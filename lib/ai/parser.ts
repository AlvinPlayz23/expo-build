// Deprecated. The agent used to emit a `<expoFile>` text protocol that this
// module parsed incrementally. The harness now uses the AI SDK's tool-calling
// loop (see lib/ai/tools.ts + lib/ai/agent.ts), so this parser is unused and
// intentionally left empty. Safe to delete.

export {};
